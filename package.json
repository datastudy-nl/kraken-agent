{
  "name": "kraken-agent-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kraken Agent documentation site",
  "scripts": {
    "docs:dev": "npx @11ty/eleventy --serve --input=docs --output=_site",
    "docs:build": "npx @11ty/eleventy --input=docs --output=_site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it-anchor": "^9.2.0"
  }
}
